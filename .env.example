# Copy to .env and adjust if needed.
# Generate a Fernet key with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# MongoDB
MONGO_URI=mongodb://admin:admin@mobility-mongodb:27017/
MONGO_DB=mobility

# Spark
SPARK_MASTER=spark://mobility-spark-master:7077

# Dataset (TLC Yellow Taxi - last month with lat/lon: 2016-06)
TLC_PARQUET_URL=https://d37ci6vzurychx.cloudfront.net/trip-data/yellow_tripdata_2016-06.parquet
TLC_SAMPLE_FRACTION=0.05
