# ─────────────────────────────────────────
#  ANCHORS
# ─────────────────────────────────────────
x-spark-worker: &spark-worker
  image: bitnamilegacy/spark:3.5.1
  # Run as host UID so writes to the bind-mounted ./data land with correct
  # ownership. GID 0 (root) preserves access to bitnami's internal dirs
  # which are group-writable to gid=0.
  user: "${HOST_UID:-1000}:0"
  environment:
    - SPARK_MODE=worker
    - SPARK_MASTER_URL=spark://mobility-spark-master:7077
    - SPARK_WORKER_MEMORY=3G
    - SPARK_WORKER_CORES=2
    - SPARK_RPC_AUTHENTICATION_ENABLED=no
    - SPARK_RPC_ENCRYPTION_ENABLED=no
    - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
    - SPARK_SSL_ENABLED=no
  volumes:
    - ./jobs:/opt/bitnami/spark/jobs
    - ./data:/opt/bitnami/spark/data
  depends_on:
    - mobility-spark-master
  networks:
    - mobility-net

x-airflow-common: &airflow-common
  build:
    context: ./airflow
    dockerfile: Dockerfile
  image: mobility-airflow:2.9.1
  # Match host UID so the Spark driver (running in this container in client
  # mode) can write to the bind-mounted ./data directory. GID 0 keeps access
  # to bitnami's group-writable internal dirs. This is the pattern used in
  # Airflow's official docker-compose quickstart.
  user: "${HOST_UID:-1000}:0"
  environment:
    - AIRFLOW__CORE__EXECUTOR=LocalExecutor
    - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@mobility-postgres/airflow
    - AIRFLOW__CORE__FERNET_KEY=${FERNET_KEY:-}
    - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=true
    - AIRFLOW__CORE__LOAD_EXAMPLES=false
    - AIRFLOW__API__AUTH_BACKENDS=airflow.api.auth.backend.basic_auth
    - AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK=true
    - PYTHONPATH=/opt/airflow/plugins
    - SPARK_MASTER=spark://mobility-spark-master:7077
    - MONGO_URI=mongodb://admin:admin@mobility-mongodb:27017/
  volumes:
    - ./airflow/dags:/opt/airflow/dags
    - mobility-airflow-logs:/opt/airflow/logs
    - ./airflow/plugins:/opt/airflow/plugins
    - ./jobs:/opt/airflow/jobs
    - ./data:/opt/airflow/data
    # Mirror Spark cluster paths so the driver (running in this container
    # in --master client mode) resolves the same absolute paths the workers
    # see. Required by SparkSubmitOperator -> spark.read on these inputs.
    - ./jobs:/opt/bitnami/spark/jobs
    - ./data:/opt/bitnami/spark/data
  depends_on:
    mobility-postgres:
      condition: service_healthy
  networks:
    - mobility-net

# ─────────────────────────────────────────
#  SERVICES
# ─────────────────────────────────────────
services:

  # ── Spark Cluster ──────────────────────────────────────────
  mobility-spark-master:
    image: bitnamilegacy/spark:3.5.1
    container_name: mobility-spark-master
    hostname: mobility-spark-master
    user: "${HOST_UID:-1000}:0"
    environment:
      - SPARK_MODE=master
      - SPARK_RPC_AUTHENTICATION_ENABLED=no
      - SPARK_RPC_ENCRYPTION_ENABLED=no
      - SPARK_LOCAL_STORAGE_ENCRYPTION_ENABLED=no
      - SPARK_SSL_ENABLED=no
    ports:
      - "8080:8080"
      - "7077:7077"
    volumes:
      - ./jobs:/opt/bitnami/spark/jobs
      - ./data:/opt/bitnami/spark/data
    networks:
      - mobility-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 5

  mobility-spark-worker-1:
    <<: *spark-worker
    container_name: mobility-spark-worker-1
    hostname: mobility-spark-worker-1
    ports:
      - "8081:8081"

  mobility-spark-worker-2:
    <<: *spark-worker
    container_name: mobility-spark-worker-2
    hostname: mobility-spark-worker-2
    ports:
      - "8082:8081"

  # ── Airflow metadata DB ────────────────────────────────────
  mobility-postgres:
    image: postgres:15-alpine
    container_name: mobility-postgres
    hostname: mobility-postgres
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - mobility-postgres-data:/var/lib/postgresql/data
    networks:
      - mobility-net
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Airflow init ───────────────────────────────────────────
  mobility-airflow-init:
    <<: *airflow-common
    container_name: mobility-airflow-init
    # NOTE: do not override the official /entrypoint -- it adds the runtime
    # UID to /etc/passwd, which Airflow CLI requires (getuser()).
    command:
      - bash
      - -c
      - |
        set -e
        airflow db migrate
        airflow users create \
          --username admin \
          --firstname Admin \
          --lastname User \
          --role Admin \
          --email admin@local.dev \
          --password admin || true
        airflow connections add spark_default \
          --conn-type spark \
          --conn-host spark://mobility-spark-master \
          --conn-port 7077 || true
        echo "Init complete"
    restart: on-failure

  mobility-airflow-webserver:
    <<: *airflow-common
    container_name: mobility-airflow-webserver
    command: webserver
    ports:
      - "8090:8080"
    depends_on:
      mobility-airflow-init:
        condition: service_completed_successfully
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  mobility-airflow-scheduler:
    <<: *airflow-common
    container_name: mobility-airflow-scheduler
    command: scheduler
    depends_on:
      mobility-airflow-init:
        condition: service_completed_successfully
    restart: unless-stopped

  # ── MongoDB ────────────────────────────────────────────────
  mobility-mongodb:
    image: mongo:7
    container_name: mobility-mongodb
    hostname: mobility-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
      MONGO_INITDB_DATABASE: mobility
    ports:
      - "27017:27017"
    volumes:
      - mobility-mongo-data:/data/db
    networks:
      - mobility-net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ── Streamlit ──────────────────────────────────────────────
  mobility-streamlit:
    build:
      context: ./streamlit
      dockerfile: Dockerfile
    container_name: mobility-streamlit
    ports:
      - "8501:8501"
    volumes:
      - ./streamlit:/app
    environment:
      - MONGO_URI=mongodb://admin:admin@mobility-mongodb:27017/
      - MONGO_DB=mobility
    depends_on:
      mobility-mongodb:
        condition: service_healthy
    networks:
      - mobility-net
    restart: unless-stopped

  # ── Jupyter ────────────────────────────────────────────────
  mobility-jupyter:
    build:
      context: ./notebooks
      dockerfile: Dockerfile
    image: mobility-jupyter:3.5.1
    container_name: mobility-jupyter
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - SPARK_MASTER=spark://mobility-spark-master:7077
      - MONGO_URI=mongodb://admin:admin@mobility-mongodb:27017/
    volumes:
      - ./notebooks:/home/jovyan/work
      - ./data:/home/jovyan/data
    depends_on:
      - mobility-spark-master
    networks:
      - mobility-net

# ─────────────────────────────────────────
#  VOLUMES & NETWORKS
# ─────────────────────────────────────────
volumes:
  mobility-postgres-data:
  mobility-mongo-data:
  mobility-airflow-logs:

networks:
  mobility-net:
    driver: bridge
